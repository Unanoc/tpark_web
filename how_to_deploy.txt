sudo apt-get update
sudo apt-get upgrade

sudo apt-get install python3
sudo apt-get install nginx

sudo apt-get install python3-setuptools
sudo easy_install3 pip

cd /home/
git clone https://github.com/Unanoc/tpark_web.git
cd tpark_web/
pip3 install virtualenv
virtualenv venv
source venv/bin/activate

pip install django==1.11
pip install gunicorn

sudo apt-get install mysql-server mysql-client

mysql -u root -p
CREATE DATABASE ask_me DEFAULT CHARACTER SET utf8 DEFAULT COLLATE utf8_general_ci;
CREATE USER 'django'@'localhost' IDENTIFIED BY 'password like in config.cnf';
GRANT ALL PRIVILEGES ON 'ask_me'.* TO 'django'@'localhost';
FLUSH PRIVILEGES;

CTRL + D

sudo apt-get install python3-dev libmysqlclient-dev build-essential
pip install mysqlclient

python manage.py makemigrations
python manage.py migrate

cd question_ask_project (dir where manage.py is located)
gunicorn question_ask_project.wsgi:application --bind xx.xx.xx.xx:8000   (without static)

cd /etc/nginx/sites-available/
vim default

"""
    server {
        listen 80;
        server_name 64.52.87.170; #либо ip, либо доменное имя
        access_log  /var/log/nginx/ask_me.log;

        location /static/ {
            root /home/tpark_web/question_ask_project/ask_me/;
            expires 30d;
        }

        location /media/ {
        root /home/tpark_web/question_ask_project/;
        }

        location / {
            proxy_pass http://127.0.0.1:8000;
            proxy_set_header Host $server_name;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }
    }
"""

Insert "client_max_body_size" in nginx.conf (http { })

sudo service nginx restart

alias runsite='source /home/tpark_web/venv/bin/activate && cd /home/tpark_web/question_ask_project && screen gunicorn question_ask_project.wsgi:application'

(In settings.py:   debug = False)